version: 2

sources:
  - name: stripe
    database: raw
    tables:
      - name: payment
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after:
              count: 17
              period: day
            error_after:
              count: 18
              period: day